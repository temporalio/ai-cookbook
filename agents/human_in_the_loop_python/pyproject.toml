[project]
name = "human-in-the-loop-python"
version = "0.1.0"
description = "Human-in-the-loop AI agent with Temporal"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "temporalio>=1.8.0",
    "boto3>=1.35.0",
    "botocore[crt]>=1.35.0",
    "pydantic>=2.10.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["models", "activities", "workflows"]
