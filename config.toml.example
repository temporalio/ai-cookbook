# Temporal Client Configuration for AI Cookbook
# This file defines profiles for different Temporal environments.
# By default, recipes will use the "default" profile for local development.
# To use Temporal Cloud, specify the "cloud" profile when loading the config.

# Default profile for local development
[profile.default]
address = "localhost:7233"
namespace = "default"

# Cloud profile for Temporal Cloud
[profile.cloud]
address = "your-namespace.a1b2c.tmprl.cloud:7233"
namespace = "your-namespace"
# Replace with your actual Temporal Cloud API key
api_key = "your-api-key-here"

# TLS configuration for production
[profile.cloud.tls]
# TLS is auto-enabled when an API key is present, but you can configure it
# explicitly.
# disabled = false

# Use certificate files for mTLS. Replace with actual paths.
# client_cert_path = "/etc/temporal/certs/client.pem"
# client_key_path = "/etc/temporal/certs/client.key"
