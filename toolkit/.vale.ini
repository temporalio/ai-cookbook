# ABOUTME: Vale config for the AI Cookbook prose layer. Small by design.
# Applied to recipe README.md files; the AICookbook style lives in styles/.

StylesPath = styles
MinAlertLevel = suggestion
Vocab = AICookbook

[**/README.md]
BasedOnStyles = AICookbook
